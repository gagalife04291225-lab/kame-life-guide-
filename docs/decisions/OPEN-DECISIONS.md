# 裁定事項 — Owner の判断を待っているもの

> **Conforms-to: Development Constitution v2.1**
>
> `.claude/rules/pm-conduct.md` §8.7「Owner 判断事項を作業報告に埋めない」に従い、
> 裁定が要る事項をここに分離する。**作業報告の本文には書かない。**
>
> 各項目は次を必ず持つ — 競合している規則（条番号）／選択肢（最低2つ・影響つき）／
> 推奨と理由／**裁定が無い場合に何が止まるか**。
> 裁定が済んだ項目はこのファイルから削除し、確定内容を該当規則へ反映する。
>
> 索引は `docs/AI-HANDOFF.md` の UNRESOLVED にある。

---

## D-01 — Short Video Gate のデッドロック

### 対象の規則

`.claude/rules/pm-conduct.md` §6 — 自前撮影なしの場合、正式APIキーまたはローカル配置で
取得した**商用利用可能で権利確認可能な HD 以上のカメ生体実写映像**が無ければ、
動画レンダリングを完成扱いにしない。

### 2026-09-03 追加実測 — 前提が変わった（素材源サーベイ）

`ai-tiktok-company` の `experiments/kame-source-survey/SOURCES.md`（実測・runner 上で取得）:

- Wikimedia Commons のカメ動画を**ソース単位で数え直した**（鍵不要の公式 API・per-item ライセンス取得）
  → **176件**（video mime・ウミガメ/AI除外）。**全件が商用利用可。HD以上152件。最大 3840×2160。実DL成功**
- ライセンス内訳: Public domain 74 / CC0 5 / CC BY 系 50 / CC BY-SA 系 47
  → **継承義務が無いものが129件ある**
- HD上位を実際にダウンロードして**全部目視した**結果の歩留まり:
  **PD/CC0 = 3/14（21%）**、**CC BY（継承なし）= 9/12（75%）**
- **水槽の中で餌を食べる個体の水中接写（1920×1080 / CC BY 3.0）が存在した。**
  第2弾で「Restricted License でしか手に入らない」と結論していた"飼育シーンそのもの"である

**したがって「§6 を満たす素材が存在しない」という D-01 の前提は成立しない。**
動画制作を始めるために CC BY-SA の継承条件を受け入れる必要は無い。

なお「176件」は使える本数ではない。目視した14件中7件はカメが1匹も写っておらず、
キーワード一致は実物を保証しない。**目視ゲートを工程に入れることが前提条件になる。**

### 影響（第2弾時点の実測・上記により一部が過去のものになった）

素材監査（`experiments/kame-video-audit/AUDIT.md`・別リポジトリ）の結論:

- 台帳1,000スロット（Pexels / Pixabay）は **BLOCKED_API_KEY**。
  実測 — Pexels `HTTP 401 Missing API key` / Pixabay `HTTP 400 Invalid or missing API key`
- 実体297件を個別監査、95件を取得・全尺確認
- Mixkit のカメ動画22本は **全て Restricted License（personal use only）** で収益化不可
- **日本の飼育主要種**（クサガメ・ニホンイシガメ・ニオイガメ・チズガメ・ハコガメ類）で
  見どころがあり商用利用可の実写は**ほぼ存在しなかった**
- 商用可で強かったのはリクガメ（ケヅメ／サバクゴファー）とスライダー類に偏る

**自前撮影をしない方針**と合わせると、現状の素材調達のままでは
**動画は永久に完成扱いにできない。**

### 選択肢

| 案 | 内容 | 影響 |
|----|------|------|
| **E（新）** | **Wikimedia Commons の CC BY（継承なし）を主レーンに固定して制作を再開する** | 鍵不要・$0・per-item ライセンスが機械取得可・1080p。**BY-SA の継承義務を負わない**。帰属表記は作者名がAPIで取れるため SHORTS GATE の G10 で機械検査できる |
| **A** | Pexels / Pixabay / Flickr の無料APIキーを取得し repo secret へ登録 | 母数が増える。**固定先を変える必要はない**（Commons を主・鍵付きストックを副）。Owner のアカウント作成が必要 |
| **B** | CC BY-SA 4.0 の継承条件を受け入れる | 第2弾TOP1（ケヅメが草を食べる超接写）とTOP3（幼体スライダー）が使えるが、**完成動画も BY-SA で公開する義務**が生じる。**上記実測により、制作開始の必須条件ではなくなった** |
| **C** | 動画制作を一旦停止する | サイト本体の改善に集中する |
| **D** | §6 を緩める | **推奨しない**。緩めれば低品質な成果物が「完成」になる |

### 推奨

**E 単独で再開できる。** A は任意（母数を増やすだけで、固定先の変更を伴わない）。
**B は保留してよい** — 継承義務を負わずに始められることが実測で分かったため、
BY-SA は「どうしてもその1本が要るとき」に個別判断すればよい。D は採らない。
§6 は Owner が確定した規範であり、実現できないから緩めるのは
`pm-conduct.md` §4「結果を見て合格基準を下げない」に反する。

**E を採る場合の必須条件（実測にもとづく）**: 目視ゲートを工程に入れること。
機械一致だけで採用すると、カメが写っていない素材が約半数混入する。

### 裁定が無い場合に止まること

**動画制作が止まったままになる。** ただし止まる理由はもう「素材が無いから」ではなく、
**どのレーンで再開するかが決まっていないから**である。

---


## D-02 — PM 実務原則（§8）の承認

`.claude/rules/pm-conduct.md` §8 は **Claude Code 起案**（Owner の指示による）であり、
§1〜§7（Owner 確定）とは出所が異なる。§8 冒頭にその旨を明記済み。

7原則 — 8.1 Artifact First／8.2 反証可能な受入基準／8.3 PM 自身の数値にも出所／
8.4 STOP を成果として扱う／8.5 一度に1工程／8.6 自著の唯一の判定者にならない／
8.7 Owner 判断事項を作業報告に埋めない

### 選択肢

| 案 | 内容 |
|----|------|
| **A** | 全7原則を承認し、§1〜§7 と同格の規範に格上げする |
| **B** | 実務原則のまま維持する（上位規範と競合すれば §1〜§7 と憲法が優先） |
| **C** | 一部を削除・修正する |

### 推奨

**B**。§8 は本件1セッションの観察にもとづく。**格上げは複数案件で有効性を確認してからでよい。**
ただし **8.1（Artifact First）だけは A** を推す。今日の重大欠陥3件すべてを検出した唯一の手段であり、
根拠が実測である。

### 裁定が無い場合に止まること

止まらない。§8 は実務原則として機能する。

---

## （裁定済み・記録）D-05 / D-06 — kame ゲートの較正と音声の扱い

**2026-09-04 Owner 裁定 = D-05 案A / D-06 案B。** 実装済み。**閾値は一度も下げていない。**

### D-05（案A・K1 の較正）— 較正の結果、K1 を廃止した

9:16 の0秒相当フレーム12件（正例4＝目視で顔と目が見える／負例8＝目視でカメ不在・黒・単色）で
候補指標×候補閾値を総当たりした結果、**完全判別する設定は 0 件**だった。

| 例 | A_col_sd_max | 期待 |
|---|---|---|
| P1 完成物0.0s | 31.39 | PASS |
| **N1 散水パイプ（カメ不在）** | **55.29** | FAIL のはず |
| **N4 バケツ運搬（カメ不在）** | **56.89** | FAIL のはず |
| N7 黒画面 | 0.35 | FAIL |

**正例より負例のほうが値が大きく、順序が逆転している。** どんな閾値でも分離できない。
負例は「カメ不在だが人物・カゴ・バケツ・箱・テープ台が写る」映像で、輝度もエッジも豊富だったため。
黒画面・単色だけは落とせるが、それは **G3 が既に担っており追加の判別力はゼロ**。

→ **判別力のない検査を廃止し**、担保を **K2（宣言＋**目視の記録**）** へ移した。
K2 は `visual_check` に「誰が・いつ・どのフレームを見て・どう判定したか」を要求し、
**フレームのファイル実在まで検査する**。記録が無ければ FAIL。
案A の但し書き「較正できない場合は B（宣言＋目視）へ退避する」に該当する。

生データ: `ai-tiktok-company` の `qc-evidence/shorts-gate/k1-calib/k1_calib.json`

### D-06（案B・G2 を K8 へ置換）

kame で G2 を非適用にし、**K8「音声状態の宣言と実測一致」** を新設した。

- 原素材の音声状態を**実測して宣言**する（可聴閾値 −50 dB）
- 原素材が可聴なら**原音を維持**（完成物が無音なら FAIL）
- 原素材が真正な無音なら**無音を許容**（完成物が可聴なら＝音を足したので FAIL）
- **BGM・ナレーションでの穴埋めは FAIL**
- **「音声トラックあり」（G1）と「実際に可聴」（K8）は別判定**

### 結果

**PLAYBOOK⑦ は kame ゲート全13項目 PASS（終了コード 0）。公開可能な状態にした。**
science 回帰も PASS（12ケース完全一致・適用ゲート集合の差分ゼロ）。
公開（Publish）の実施は Owner のみ（憲法 §2.6-I1）。手順は `video-output/kame-short-01/PUBLISH.md`。

---
## （裁定済み・記録）D-04 — SHORTS GATE のジャンル別プロファイル化

**2026-09-04 Owner 裁定 = 案A。** 実装済み。

- `criteria.json`: 既存 `BLOCK`（12ゲート）は**一切変更せず**、`PROFILES` と `BLOCK_KAME` を追加
- `science` = 従来12ゲート（**既定**）／`kame` = G4・G6・G12・G10 を非適用にし K1〜K7 を追加
- `gate.py`: `--profile` と `plan.profile` に対応。**既定は science** のため既存呼び出しは挙動不変
- **science 回帰 PASS**: 改定前の `gate.py` を `git show` で取り出し、同一入力12ケースで比較 →
  **判定・落ちた項目とも完全一致（不一致0件）／適用ゲート集合の差分ゼロ**
- 記録: `experiments/shorts-gate/regression.py` と `qc-evidence/shorts-gate/regression.json`

同日、**D-01 = 案E**（素材は Commons CC BY を主レーン／大量幼体の農場素材は不採用）も確定した。

---
## （裁定済み・記録）過去の Actor merge 4件の扱い

**2026-09-03 の Owner 裁定は最終的に案A（憲法 v2.1 で条件付き Actor merge を許可）となった。**
これにより、下記4件は**改定後の憲法 §2.6-I1 の5条件に照らして評価できる**。

| PR | 内容 | merge commit | 5条件の充足 |
|----|------|--------------|------------|
| #129 | PM 行動規範の恒久ルール化 | `877667d` | 文書のみ・Scope Lock外0件・破壊的変更なし → **充足** |
| #130 | AI-HANDOFF 分割と読む順序の階層化 | `1e7ec89` | 同上・非空行の欠落0を実測 → **充足** |
| #131 | PM 実務原則 §8 追加と裁定書の分離 | `321ebe0` | 同上 → **充足** |
| #132 | 裁定 D-01(案B)/D-02/D-03 の規則反映 | `5f96d2d` | 同上。Owner の明示指示による merge → **充足** |

いずれも HTML / CSS / JS / data / assets / generator / workflow は未変更。
**revert の必要は無い。** 本項は経緯の記録として残す。

> 経緯: 当初 Owner は案B（Actor は merge しない）を裁定し、規則へ反映した（PR #132）。
> その後 Owner が「危険が無ければ Actor が merge してよい」を意図していたことが分かり、
> **案A へ変更**して憲法 v2.1 の改定に至った。
