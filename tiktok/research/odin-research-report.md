# ODIN Research Department 日次レポート — 2026-07-31

調査対象: **203件** / カテゴリ 58種
測定カバレッジ: topics 117/203 (57.6%) ／ license 7/203 (3.4%)

## 判定サマリ

| 判定 | 件数 |
|------|------|
| 採用候補（未充足領域） | 12 |
| 比較検証の対象 | 0 |
| 保留（現行で足りている） | 186 |
| 不採用（自動除外） | 5 |

## 採用候補（現行構成の穴を埋めるもののみ）

| repo | カテゴリ | score | ライセンス | 導入難易度 | 理由 |
|------|---------|-------|-----------|-----------|------|
| neuml/txtai | ベクトル検索/RAG | 79.7 | 未測定 | 低 | 現行構成に無い領域: ベクトル検索（動画本数が増えて類似検索が必要になった場合のみ。現時点は SQL で足りる） |
| thedotmack/claude-mem | 知識DB/ベクトル検索 | 78.8 | 未測定 | 中 | 現行構成に無い領域: ベクトル検索（動画本数が増えて類似検索が必要になった場合のみ。現時点は SQL で足りる） |
| supabase/supabase | DB/ベクトル検索 | 76.6 | 未測定 | 中 | 現行構成に無い領域: ベクトル検索（動画本数が増えて類似検索が必要になった場合のみ。現時点は SQL で足りる） |
| RyanCodrai/turbovec | ベクトル検索 | 73.5 | 未測定 | 低 | 現行構成に無い領域: ベクトル検索（動画本数が増えて類似検索が必要になった場合のみ。現時点は SQL で足りる） |
| clpeng/Awesome-Face-Forgery-Generation-and-Detection | AI生成判定 | 69.0 | 未測定 | 中 | 現行構成に無い領域: AI生成判定（実写/CG/AI生成の割合が未測定。分類器が必要） |
| RVC-Project/Retrieval-based-Voice-Conversion-WebUI | 音楽解析 | 68.9 | 未測定 | 中 | 現行構成に無い領域: 音楽解析（BPM推定の確度が低い。ただし主要OSS(essentia)がAGPLで不適合） |
| libAudioFlux/audioFlux | 音楽解析 | 64.7 | 未測定 | 中 | 現行構成に無い領域: 音楽解析（BPM推定の確度が低い。ただし主要OSS(essentia)がAGPLで不適合） |
| Daisy-Zhang/Awesome-Deepfakes-Detection | AI生成判定 | 63.4 | 未測定 | 中 | 現行構成に無い領域: AI生成判定（実写/CG/AI生成の割合が未測定。分類器が必要） |
| CPJKU/madmom | 音楽解析 | 62.9 | 未測定 | 低 | 現行構成に無い領域: 音楽解析（BPM推定の確度が低い。ただし主要OSS(essentia)がAGPLで不適合） |
| selimsef/dfdc_deepfake_challenge | AI生成判定 | 57.1 | 未測定 | 中 | 現行構成に無い領域: AI生成判定（実写/CG/AI生成の割合が未測定。分類器が必要） |
| abhijithjadhav/Deepfake_detection_using_deep_learning | AI生成判定 | 56.7 | 未測定 | 中 | 現行構成に無い領域: AI生成判定（実写/CG/AI生成の割合が未測定。分類器が必要） |
| dessa-oss/DeepFake-Detection | AI生成判定 | 55.6 | 未測定 | 中 | 現行構成に無い領域: AI生成判定（実写/CG/AI生成の割合が未測定。分類器が必要） |

## 自動除外の内訳

| 理由 | 件数 |
|------|------|
| アーカイブ済（開発終了） | 2 |
| AGPL系。WebUI提供時にソース公開義務が及びうる（★絶対条件『商用ライセンス購入禁止』とも衝突） | 2 |
| GPU必須の申告あり（絶対条件『ローカル無料構成』に反する可能性） | 1 |

## 現行構成（比較の基準）

| 層 | 採用中 | 役割 | 置換可否 |
|----|--------|------|---------|
| 動画取得 | yt-dlp/yt-dlp | 動画取得 | 可 |
| 動画基盤 | FFmpeg (imageio-ffmpeg同梱) | 復号・フレーム抽出 | 不可（基盤） |
| 数値計算 | numpy | 全解析の基盤 | 不可（基盤） |
| カット解析 | 自前実装 (engine.py) | カット検出・静止検出 | 可 |
| 統計解析 | 自前実装 (stats.py) | Welch t / Pearson / FDR | 可 |
| DB | SQLite (標準) | 知識DB | 可 |
| WebUI | http.server (標準) | ダッシュボード | 可 |
| MCP | 自前実装 (mcp_server.py) | MCPサーバ | 可 |
| OCR | tesseract (任意) | 字幕抽出 | 可 |
| 音声解析 | faster-whisper (任意) | ナレーション速度 | 可 |
| 被写体解析 | mediapipe (任意) | 姿勢・表情 | 可 |
| ベクトル検索 | 未導入 | 現時点で不要と判断 | 可 |

## 未充足領域（ここを埋める候補だけが価値を持つ）

- **ベクトル検索**: 動画本数が増えて類似検索が必要になった場合のみ。現時点は SQL で足りる
- **AI生成判定**: 実写/CG/AI生成の割合が未測定。分類器が必要
- **ロゴ検出**: 学習済みロゴモデルが無い
- **音楽解析**: BPM推定の確度が低い。ただし主要OSS(essentia)がAGPLで不適合
- **感情解析**: 感情変化・驚きの位置が未測定

---
_判定はすべて実測フィールド（stars/forks/issues/更新日/archived/topics/license）由来。_
_速度・精度は未測定のためスコアに含めていない。_
